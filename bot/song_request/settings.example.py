"""Настройки song-request модуля (шаблон).

Скопируйте в settings.py. SR_COST — bootstrap до первого сохранения
во вкладке «Очередь» админки; дальше правится там.
settings.py не попадает в git и сохраняется при update.cmd.
"""

SR_COST = 100  # Стоимость !sr в принцессах; 0 = бесплатно (только seed)
