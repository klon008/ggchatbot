"""Настройки игрового баланса princess-модуля (шаблон).

Скопируйте в settings.py и меняйте под свой канал.
settings.py не попадает в git и сохраняется при update.cmd.
"""
from __future__ import annotations

from zoneinfo import ZoneInfo

# --- Время и часовой пояс ---

MSK = ZoneInfo("Europe/Moscow")  # Часовой пояс для дейликов, кражи и тюрьмы

# --- Сообщения и пассивный доход ---

MESSAGE_POINTS = 5  # Принцессы за каждое сообщение в чате
PASSIVE_INCOME_PER_MIN = 3  # Пассивный доход за минуту присутствия в чате
PASSIVE_INCOME_INTERVAL_SEC = 60  # Интервал начисления пассивного дохода (сек)

# --- Кубик (!дайс) ---

DICE_COOLDOWN_SEC = 300  # Перезарядка команды !дайс (сек)
DICE_COST = 5  # Стоимость одного броска кубика
DICE_ROLL_MIN = 1  # Минимальное значение броска d20
DICE_ROLL_MAX = 20  # Максимальное значение броска d20
DICE_CRITICAL_FAIL = 1  # Значение критического провала
DICE_CRITICAL_SUCCESS = 20  # Значение критического успеха
DICE_CRITICAL_FAIL_PENALTY = 50  # Штраф принцесс при критическом провале
DICE_CRITICAL_SUCCESS_REWARD = 200  # Награда принцесс при критическом успехе

# --- Кража (!кража) ---

STEAL_MIN_VIEWERS = 5  # Минимум зрителей в чате, чтобы кража была доступна
STEAL_COOLDOWN_SEC = 600  # Перезарядка команды !кража (сек)
STEAL_ALLOWED_WEEKDAYS = (2, 4)  # Дни недели кражи: 0=пн, 2=ср, 4=пт
STEAL_ROLL_MAX = 100  # Верхняя граница броска d100 для проверки успеха
STEAL_BANK_JACKPOT_CHANCE = 0.01  # Шанс джекпота из казны (доля 0..1; 0.01 = 1%)
STEAL_BANK_AMOUNT_MULT = 2  # Множитель суммы при джекпоте из казны
VICTIM_MIN_BALANCE = 3000  # Минимальный баланс жертвы, чтобы её можно было ограбить
STEAL_AMOUNT_FLOOR = 100  # Минимум суммы после тира (ниже — срыв без тюрьмы)

# Дефолты тиров добычи: (вес, мин, макс). Override — из админки (steal_meta.loot_tiers_json)
STEAL_LOOT_TIER_KEYS: tuple[str, ...] = ("meloch", "normal", "zhir", "kush")
STEAL_LOOT_TIERS: tuple[tuple[int, int, int], ...] = (
    (50, 100, 400),   # meloch / мелочь
    (30, 400, 900),   # normal / норма
    (15, 900, 1600),  # zhir / жир
    (5, 1600, 2500),  # kush / куш
)

# --- Рост / спад шанса кражи ---

STEAL_CHANCE_FLOOR = 5  # Пол шанса %
STEAL_CHANCE_CAP = 35  # Потолок шанса %
STEAL_CHANCE_ATTEMPTS_DIV = 5  # +1% шанса каждые N попыток !кража
STEAL_DECAY_MISSED_DAY_PCT = 2  # −% за пропуск ср/пт без !кража

# --- Тюрьма ---

PRISON_DURATION_SEC = 30 * 60  # Длительность заключения (сек)

# Шанс попасть в тюрьму после реальной кражи: (мин. сумма, макс. сумма, шанс %)
PRISON_CHANCE_TIERS: tuple[tuple[int, int, int], ...] = (
    (100, 399, 5),
    (400, 899, 12),
    (900, 1599, 20),
    (1600, 2500, 30),
    (2501, 5000, 35),  # джекпот из казны
)

# --- Ежедневный бонус (!дейлик) ---

DAILY_BONUS_DEFAULT = 15000  # Бонус за день > 25 в текущем месяце

DAILY_BONUS_MAP: dict[int, int] = {
    1: 100,
    2: 100,
    3: 100,
    4: 150,
    5: 2000,
    6: 150,
    7: 200,
    8: 200,
    9: 200,
    10: 5000,
    11: 250,
    12: 250,
    13: 250,
    14: 300,
    15: 7000,
    16: 300,
    17: 350,
    18: 350,
    19: 350,
    20: 10000,
    21: 400,
    22: 400,
    23: 400,
    24: 450,
    25: 15000,
}
