# TODO

## Настройки модулей перезатираются при `git pull`

**Файлы:** `bot/princess/settings.py`, `bot/song_request/settings.py`

Сейчас это обычные Python-модули в репозитории с балансом игры (принцессы, кража, дейлики, стоимость `!sr` и т.д.). Стример или разработчик может менять их под свой канал, но при каждом `update.cmd` / `git pull` локальные правки будут **перезаписаны** версией из GitHub.

**Нужно продумать:**

- [x] Как отделить «дефолты из репо» от «локальных настроек канала»
- [x] Варианты (выбрать один или комбинацию):
  - `settings.example.py` в git + `settings.py` в `.gitignore` (как `.env`)
  - `settings_local.py` / `local_settings.py`, который импортируется поверх дефолтов и не в git
  - вынести тюнинг в `.env` или `data/config.json` (уже не в git)
  - при `update.ps1` — предупреждение или бэкап, если `settings.py` изменён локально
- [x] Обновить `README` / `ИНСТРУКЦИЯ.txt`: где править баланс и что сохранится после обновления
- [x] Миграция для уже установленных копий (у кого уже есть свои `settings.py`)

## Возврат принцесс, если песня не заказалась

**Сейчас:** при `!sr` принцессы списываются сразу при добавлении в очередь (`handler.py`), но при ошибке воспроизведения (`error`, `too_long`, таймаут watchdog, недоступность YouTube API) возврата нет — зритель теряет `SR_COST`, хотя трек не проиграл.

**Нужно продумать:**

- [x] Когда именно возвращать: только если трек так и не начал играть, или также при сбое в первые N секунд?
- [x] Случаи без возврата: модераторский `!skip`, успешное воспроизведение до конца
- [x] Хранить в `Track` флаг/сумму списания (`paid_cost`, `requested_by`) для возврата при `advance(..., skip_reason=...)`
- [x] Сообщение в чат: «@{user}, возвращено N принцесс — {причина}»
- [x] Учесть `SR_COST = 0` (бесплатные заказы) и отсутствие `PointsStore`
